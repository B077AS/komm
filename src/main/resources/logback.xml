<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <conversionRule conversionWord="customLevel" class="komm.utils.CustomDebugConfig" />

    <!-- Resolve AppData path via system property set in Launcher -->
    <property name="LOG_DIR" value="${komm.logDir:-logs}" />

    <!-- Console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %customLevel(%-5level) %cyan(%logger{30}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Single app log -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/komm.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/komm.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>15</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- Your app packages -->
    <logger name="komm" level="DEBUG" />

    <!-- Silence noisy third-party libs -->
    <logger name="dev.onvoid.webrtc" level="WARN" />
    <logger name="org.glassfish.tyrus" level="WARN" />

    <!-- Root -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
